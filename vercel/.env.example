# Optional. Without it the app still uses the free XposedOrNot and LeakCheck
# endpoints. Set it in Vercel under Settings -> Environment Variables.
HIBP_API_KEY=

# Per-request HTTP timeout, seconds
HTTP_TIMEOUT=8

# Total scan budget. Must stay below the maxDuration in vercel.json.
SCAN_BUDGET=45
