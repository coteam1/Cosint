# --- core web app ---
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
httpx>=0.26
pydantic>=2.6
python-dotenv>=1.0

# --- built-in module dependencies ---
dnspython>=2.6
phonenumbers>=8.13

# NOTE: holehe / maigret / ghunt are intentionally NOT listed here.
# They pin conflicting versions of aiohttp, httpx and beautifulsoup4.
# The Dockerfile installs each one into its own virtualenv under /opt/venvs.
# Running outside Docker? Use pipx:
#   pipx install holehe && pipx install maigret && pipx install ghunt
